# React 19 is pinned via Next.js 15; some libraries (e.g. recharts) declare peer
# ranges that don't accept React's pre-release tag. Use the legacy peer-dep
# resolver so installs succeed without forcing every lib to update its peers.
legacy-peer-deps=true
